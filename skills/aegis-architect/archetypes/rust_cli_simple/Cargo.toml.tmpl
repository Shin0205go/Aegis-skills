[package]
name = "{{name}}"
version = "0.1.0"
edition = "2021"
description = "{{description}}"

[dependencies]
# TODO: 必要な依存を追加
# clap = { version = "4", features = ["derive"] }
# tokio = { version = "1", features = ["full"] }

[dev-dependencies]
